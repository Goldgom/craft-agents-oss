/**
 * Resource bundle file format constants shared by the AI export/import tools.
 *
 * MUST stay in sync with `RESOURCE_BUNDLE_FILE_MARKER` / version in
 * packages/shared/src/resources/resource-file.ts so AI-written archives are
 * interoperable with the manual UI export/import flow.
 */
export const RESOURCE_BUNDLE_FILE_MARKER = 'craft-resource-bundle';
export const RESOURCE_BUNDLE_FILE_VERSION = 1;
